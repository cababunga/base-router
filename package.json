{
  "name": "base-router",
  "version": "0.5.1",
  "description": "Request router and middleware stacker with async/await API and zero dependencies",
  "main": "router",
  "scripts": {
    "test": "mocha *.test.js",
    "bump": "npm version ${BUMP:=patch} && git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cababunga/base-router"
  },
  "license": "ISC"
}
